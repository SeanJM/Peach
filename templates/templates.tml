<template title>
  <h1>{{title}}</h1>
</template>

<template mlp>
  <ul>
    {{
      each items do
        mlp-list
      endeach
    }}
  </ul>
</template>

<template mlp-list>
  <li>{{if href <a href="{{href}}"> endif}}{{name}}{{if href </a> endif}}</li>
  {{
    if dog == true
      This has a dog
    endif
  }}
  {{if $index == 1 this is first endif}}
  {{if $index == 3 this is third endif}}
  {{$oddOrEven}}
</template>